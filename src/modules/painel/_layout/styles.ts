import styled from "styled-components";

export const PillContainter = styled.div`
  position: fixed;
  top : 20px;
  display: flex;
  gap: 4px;
  button {
    background: #fff;
    border-radius: 6px;
    padding: 10px;
    box-shadow: 0 0 2px 0 rgba(0, 0, 0, 0.4);
  }
`;